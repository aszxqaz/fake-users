export type User = {
    id: string;
    fullname: string;
    address: string;
    phone: string;
    locale: string;
};

export type CountryInfo = {
    name: string;
    phoneCode: string;
    nameLocal: string;
};
